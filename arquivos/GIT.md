``` sh
git config user.name "lodestaarz" 
git configu user.email "vini0377@gmail.com"
git config core.autocrlf input
git branch -m main 
git remote add origin https://github.com/lodestaarz/PROJETO-NESTJS
git add .
git commit -m "initial" 
git push origin main -u

git add. #Para adicionar código 
git commit -m "mensagem" 
git push
```